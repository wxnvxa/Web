# 🚀 Автоматическое сохранение в GitHub

Настроены скрипты для автоматического коммита и push в GitHub.

## 📋 Варианты использования:

### 1. Ручной запуск (рекомендуется)
Просто запустите один из файлов:
- **`auto-commit.bat`** - двойной клик или запуск из терминала
- **`auto-commit.ps1`** - PowerShell скрипт

**С кастомным сообщением:**
```bash
.\auto-commit.bat "Исправлены баги в навигации"
```

### 2. Автоматический push после коммита
После каждого `git commit` изменения автоматически отправятся в GitHub через git hook.

**Чтобы включить:**
- Git hook уже настроен в `.git/hooks/post-commit.bat`
- Работает автоматически после каждого коммита

### 3. Автоматическое отслеживание изменений
Запустите `watch-changes.ps1` - он будет отслеживать изменения и автоматически коммитить каждые 5 секунд.

```powershell
.\watch-changes.ps1
```

## ⚙️ Настройка

Если нужно изменить интервал отслеживания или другие параметры, отредактируйте соответствующие скрипты.

## ⚠️ Примечание

Убедитесь, что у вас настроена аутентификация GitHub (SSH ключ или Personal Access Token).

